on:
  push:
    branches:
      - master

   
    


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: " bb"
        run: ls

      - uses: actions/checkout@v3 

      - name : ' aa'
        run: ls


        

    
